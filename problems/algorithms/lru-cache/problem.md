# LRU Cache

Design a least-recently-used cache with a fixed capacity. Support `get(key)`, returning the value or -1, and `put(key, value)`, updating existing keys and evicting the least recently used key when capacity is exceeded. Both operations should be constant time.

Source: https://neetcode.io/problems/lru-cache

Implement the provided `Solution.LRUCache` class.

