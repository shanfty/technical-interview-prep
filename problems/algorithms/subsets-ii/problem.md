# Subsets II

Given an integer array that may contain duplicates, return all distinct subsets. The order of subsets is unspecified.

Source: https://neetcode.io/problems/subsets-ii

Implement `Solution.subsetsWithDup(int[] nums)`.

