# Sliding Window Maximum

Given an integer array and a window size `k`, return the maximum value in every contiguous window of size `k`, from left to right. Assume `1 <= k <= nums.length`.

Source: https://neetcode.io/problems/sliding-window-maximum

Implement `Solution.maxSlidingWindow(int[] nums, int k)`.

