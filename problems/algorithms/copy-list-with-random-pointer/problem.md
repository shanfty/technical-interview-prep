# Copy List with Random Pointer

Deep-copy a linked list where each node has a next pointer and a random pointer that may reference any node in the list or be null. The returned list must contain new nodes with the same values and pointer relationships.

Source: https://neetcode.io/problems/copy-linked-list-with-random-pointer

Implement `Solution.copyRandomList(Node head)`.

