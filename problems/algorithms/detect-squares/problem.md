# Detect Squares

Design a data structure that adds points on a 2D plane and counts the number of axis-aligned squares having a given point as one vertex. Duplicate points count as separate points.

Source: https://neetcode.io/problems/detect-squares

Implement the provided `Solution.DetectSquares` class.

