# Valid Parenthesis String

Given a string containing `(`, `) `, and `*`, determine whether it can be interpreted as a valid parenthesis string. Each asterisk may be treated as an opening parenthesis, a closing parenthesis, or an empty string.

Source: https://neetcode.io/problems/valid-parenthesis-string

Implement `Solution.checkValidString(String s)`.

