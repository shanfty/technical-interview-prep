# Add Two Numbers

Given two non-empty linked lists representing non-negative integers in reverse digit order, return their sum as a linked list in the same reverse order.

Source: https://neetcode.io/problems/add-two-numbers

Implement `Solution.addTwoNumbers(ListNode l1, ListNode l2)`.

